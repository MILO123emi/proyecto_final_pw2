{{#each faq_sections}}
    <div class="cuerpo-style">
        <h2>{{section_title}}</h2>

        {{#each questions}}
            <h3>{{question}}</h3>
            <p>{{answer}}</p>
        {{/each}}
    </div>
{{/each}}